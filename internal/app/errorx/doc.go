// Package errorx contains application-level error parsing and enhancement rules.
//
// It keeps matching and message transformation separate from Cobra command code,
// so rules are easier to test and evolve.
package errorx
